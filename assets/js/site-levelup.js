(()=>{'use strict';const clock=document.querySelector('[data-tehran-clock]');const tick=()=>{if(!clock)return;const now=new Date();clock.textContent=new Intl.DateTimeFormat('fa-IR',{timeZone:'Asia/Tehran',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(now);clock.dateTime=now.toISOString()};tick();setInterval(tick,1000);const header=document.querySelector('.header'),links=[...document.querySelectorAll('.nav a')];const current=location.pathname+location.search;links.forEach(link=>{const target=new URL(link.href,location.href);if(target.pathname+target.search===current)link.setAttribute('aria-current','page')});let last=scrollY;addEventListener('scroll',()=>{const y=scrollY;header?.classList.toggle('nav-up',y>last&&y>240);last=y},{passive:true});})();
