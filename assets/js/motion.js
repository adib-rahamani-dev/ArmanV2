(()=>{'use strict';const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)],reduced=matchMedia('(prefers-reduced-motion:reduce)').matches;if(!reduced){document.documentElement.classList.add('motion-ready');const groups=['.conversion-heading','.offer-card','.path-finder__box','.work-card','.result-strip blockquote','.result-numbers>div','.simple-process article','.package-grid article','.package-note','.learning-grid article','.faq__items details'];groups.forEach(selector=>{$$(selector,$('#conversion-main')||document).forEach((el,i)=>{el.classList.add(el.matches('.work-card,.path-finder__box')?'motion-scale':'motion-item');el.style.setProperty('--motion-delay',`${Math.min(i%4,3)*90}ms`)})});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('motion-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -45px'});$$('.motion-item,.motion-scale').forEach(el=>observer.observe(el));const art=$('.conversion-hero__art img');$('.conversion-hero__art')?.addEventListener('pointermove',e=>{if(!matchMedia('(pointer:fine)').matches||!art)return;const r=e.currentTarget.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;art.style.transform=`scale(1.018) translate(${x*7}px,${y*5}px)`});$('.conversion-hero__art')?.addEventListener('pointerleave',()=>{if(art)art.style.transform=''})}const metrics=$$('.result-numbers strong'),metricWrap=$('.result-numbers');if(metricWrap&&metrics.length){const originals=metrics.map(el=>el.textContent);const values=[38,2.1,4.9],suffixes=['٪','×',''];let done=false;const countObserver=new IntersectionObserver(([entry])=>{if(!entry.isIntersecting||done)return;done=true;const start=performance.now(),duration=reduced?0:1100;const tick=now=>{const p=duration?Math.min(1,(now-start)/duration):1,ease=1-Math.pow(1-p,3);metrics.forEach((el,i)=>{const v=values[i]*ease;el.textContent=(i?v.toLocaleString('fa-IR',{maximumFractionDigits:1}):Math.round(v).toLocaleString('fa-IR'))+suffixes[i]});if(p<1)requestAnimationFrame(tick);else{metricWrap.classList.add('counted');metrics.forEach((el,i)=>el.setAttribute('aria-label',originals[i]))}};requestAnimationFrame(tick);countObserver.disconnect()},{threshold:.45});countObserver.observe(metricWrap)}const navLinks=$$('.nav a'),sections=$$('#conversion-main section[id]');if(navLinks.length&&sections.length){const map=new Map(navLinks.map(a=>[new URL(a.href,location.href).hash.slice(1),a]));const navObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){navLinks.forEach(a=>a.classList.remove('active'));map.get(entry.target.id)?.classList.add('active')}}),{rootMargin:'-25% 0px -65% 0px'});sections.forEach(s=>navObserver.observe(s))}})();
