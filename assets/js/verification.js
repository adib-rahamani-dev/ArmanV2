(()=>{'use strict';const form=document.querySelector('[data-verification-form]');document.querySelectorAll('[data-file]').forEach(input=>input.addEventListener('change',()=>{const label=input.closest('.upload')?.querySelector('[data-file-name]');if(label&&input.files?.[0])label.textContent=input.files[0].name;}));form?.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('.submit');const message=form.querySelector('.form-message');button.disabled=true;message.textContent='';button.querySelector('span').textContent='در حال ارسال امن…';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});const data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'ارسال انجام نشد.');form.hidden=true;document.querySelector('.done').hidden=false;scrollTo({top:0,behavior:'smooth'});}catch(error){message.textContent=error.message||'ارتباط با سرور برقرار نشد.';}finally{button.disabled=false;button.querySelector('span').textContent='ارسال رمزگذاری‌شده مدارک';}});})();
