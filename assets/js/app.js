(()=>{'use strict';const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const root=document.documentElement,header=$('.header');let lastY=0;addEventListener('scroll',()=>{const y=scrollY;header?.classList.toggle('scrolled',y>20);header?.classList.toggle('hidden',y>lastY&&y>160);lastY=y},{passive:true});$('.theme-toggle')?.addEventListener('click',()=>{const t=root.dataset.theme==='dark'?'light':'dark';root.dataset.theme=t;localStorage.setItem('redt-theme',t);$('meta[name=theme-color]')?.setAttribute('content',t==='dark'?'#060607':'#ffffff')});const menu=$('.mobile-menu'),toggle=$('.menu-toggle');toggle?.addEventListener('click',()=>{const open=menu.classList.toggle('open');toggle.setAttribute('aria-expanded',open);menu.setAttribute('aria-hidden',!open);document.body.style.overflow=open?'hidden':''});$$('.mobile-menu a').forEach(a=>a.addEventListener('click',()=>toggle.click()));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(el=>io.observe(el));const track=$('.slider__track'),cards=$$('.project-card'),progress=$('.slider-progress i');if(track){let index=0,start=0,drag=false;const visible=()=>innerWidth<721?1:innerWidth<1051?2:3;const move=()=>{const max=Math.max(0,cards.length-visible());index=Math.max(0,Math.min(index,max));const w=cards[0].getBoundingClientRect().width+22;track.style.transform=`translateX(${index*w}px)`;progress.style.width=`${((index+visible())/cards.length)*100}%`};$$('[data-slide]').forEach(b=>b.addEventListener('click',()=>{index+=b.dataset.slide==='next'?1:-1;move()}));track.addEventListener('pointerdown',e=>{drag=true;start=e.clientX;track.setPointerCapture(e.pointerId)});track.addEventListener('pointerup',e=>{if(!drag)return;const d=e.clientX-start;if(Math.abs(d)>45)index+=d<0?-1:1;drag=false;move()});addEventListener('resize',move)}$$('[data-testimonial]').forEach(b=>b.addEventListener('click',()=>{$$('.testimonial-box blockquote').forEach((q,i)=>q.classList.toggle('active',i==b.dataset.testimonial));$$('[data-testimonial]').forEach(x=>x.classList.toggle('active',x===b))}));const comp=$('[data-comparison]');if(comp){const range=$('input',comp),before=$('.comparison__before',comp),handle=$('button',comp);range.addEventListener('input',()=>{before.style.right=`${range.value}%`;handle.style.right=`${range.value}%`})}$$('[data-count]').forEach(el=>{const obs=new IntersectionObserver(([e])=>{if(!e.isIntersecting)return;const n=+el.dataset.count,t=performance.now(),tick=now=>{const p=Math.min(1,(now-t)/1300);el.textContent=Math.round(n*(1-Math.pow(1-p,3))).toLocaleString('fa-IR');if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);obs.disconnect()});obs.observe(el)});const modal=$('.modal');$('[data-modal]')?.addEventListener('click',()=>{modal.hidden=false;$('.modal__close',modal).focus()});const close=()=>{if(modal)modal.hidden=true};$('.modal__close')?.addEventListener('click',close);$('.modal__backdrop')?.addEventListener('click',close);addEventListener('keydown',e=>{if(e.key==='Escape')close()});const form=$('.consult-form'),toast=$('.toast');const notify=m=>{toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),4000)};form?.addEventListener('submit',async e=>{e.preventDefault();$$('label small',form).forEach(x=>x.textContent='');const btn=$('button[type=submit]',form);btn.disabled=true;btn.textContent='در حال ارسال…';try{const r=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}}),data=await r.json();if(data.errors)Object.entries(data.errors).forEach(([k,v])=>{const input=form.elements[k];if(input)input.closest('label').querySelector('small').textContent=v});notify(data.message);if(data.ok)form.reset()}catch{notify('ارتباط برقرار نشد؛ دوباره تلاش کنید.')}finally{btn.disabled=false;btn.innerHTML='ارسال درخواست <span>←</span>'}})})();
